// Package spot is the Spot SDK — a Go client for the Spot reservation platform.
//
// For the CLI, see cmd/spot.
package spot

// Version is the SDK version. Set by -ldflags at build time; defaults to "dev"
// for development builds installed via `go install` or `go build` without flags.
var Version = "dev"
